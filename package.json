{
  "private": true,
  "scripts": {
    "dev": "esbuild --sourcemap --bundle --outfile=bundle.js --watch index.js",
    "build": "esbuild --sourcemap --minify --bundle --outfile=bundle.js index.js"
  },
  "dependencies": {
    "@babel/eslint-parser": "^7.15.0",
    "enable3d": "^0.23.0",
    "esbuild": "^0.12.22",
    "eslint": "^7.32.0",
    "holoplay": "^1.0.3",
    "leapjs": "^1.1.1"
  }
}
